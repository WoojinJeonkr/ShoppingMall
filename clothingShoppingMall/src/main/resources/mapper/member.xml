<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">

	<select id="login" parameterType="memberVO" resultType="memberVO">
		select * from member where id = #{user_id} and pw = #{user_pw}
	</select>

	<insert id="create" parameterType="memberVO">
		insert into member values (#{user_id}, #{user_pw}, #{user_name}, #{user_tel}, #{user_age}, #{user_addr})
	</insert>

	<select id="idCheck" parameterType="memberVO"  resultType="memberVO">
		select * from member where id = #{user_id} 
	</select>
</mapper>
