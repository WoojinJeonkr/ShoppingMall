<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">

	<select id="login" parameterType="memberVO" resultType="memberVO">
		select * from member where user_id = #{user_id} and user_pw = #{user_pw}
	</select>

	<insert id="create" parameterType="memberVO">
		insert into member values (#{user_id}, #{user_pw}, #{user_name}, #{user_age}, #{user_tel}, #{user_tag}, #{user_addr}, #{level})
	<!-- db에 있는 컬럼 전부 써줘야한다. -->
	</insert>

	<select id="idCheck" parameterType="memberVO"  resultType="memberVO">
		select * from member where user_id = #{user_id} 
	</select>
	
	<update id="update" parameterType="memberVO">
		update member set ( #{user_pw}, #{user_name}, #{user_tel}, #{user_age}, #{user_addr})
	</update>

</mapper>
